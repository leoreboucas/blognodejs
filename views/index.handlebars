<div class="bg-light p-5 rounded-lg m-3">
    <h1 class="display-4">Bem-vindo ao Blog do Node</h1>
    <p class="lead">Este é um Blog simples, desenvolvido com Node.js + Express + MongoDB</p>
    <hr class="my-4">
    <p>Aqui você vai poder ler sobre as novidades do mundo da computação.</p>
    <a class="btn btn-primary btn-lg" href="/users/register" role="button">Crie uma conta</a>
</div>
<hr>
<h2>Postagens recentes: </h2>
{{#each post}}
<div class="card mt-4">
    <div class="card-body">
        <small>Categoria: {{category.name}}</small>
        <br>
        <h4>{{title}}</h4>
        <p>Descrição: {{description}}</p>
        <a href="/post/{{slug}}">
            <button class="btn btn-primary">Leia mais</button>
        </a>
        <hr>
        <small>Data de publicação: {{date}}</small>
    </div>
</div>
{{else}}
    <h4 class="mt-4">Nenhuma postagem registrada</h4>
{{/each}}